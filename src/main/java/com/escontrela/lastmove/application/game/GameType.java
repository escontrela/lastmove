package com.escontrela.lastmove.application.game;

/** Identifies the consumer/workflow that owns the game-specific configuration. */
public enum GameType {
  HUMAN_VS_COMPUTER,
  LICHESS_BOT_TOURNAMENT
}
