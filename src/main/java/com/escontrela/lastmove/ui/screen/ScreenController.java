package com.escontrela.lastmove.ui.screen;

import javafx.stage.Stage;

/**
 * Manages the lifecycle of a JavaFX screen (scene or view).
 *
 * <p>Placeholder – multi-screen navigation support is planned for a future milestone.
 */
public interface ScreenController {

    /**
     * Activates this screen on the given stage.
     *
     * @param stage the primary stage
     */
    void show(Stage stage);
}
